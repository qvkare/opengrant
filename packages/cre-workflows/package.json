{
  "name": "@opengrant/cre-workflows",
  "version": "0.1.0",
  "description": "Placeholder: Chainlink CRE workflows for payment verification. Requires CRE credentials and DON ID for deployment (Phase 3).",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "simulate": "cre workflow simulate",
    "deploy": "cre workflow deploy"
  },
  "dependencies": {
    "viem": "^2.45.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  }
}
