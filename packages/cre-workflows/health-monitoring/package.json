{
  "name": "health-monitoring",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "cre-compile": "bun build src/index.ts --target=wasi --compile --outfile tmp.wasm"
  },
  "dependencies": {
    "@chainlink/cre-sdk": "^1.0.9",
    "@chainlink/cre-sdk-javy-plugin": "^1.0.9"
  }
}
